import { describe, expect, it } from 'vitest'

// Placeholder until real engine tests land - keeps `vitest run` from failing
// on an empty suite in CI.
describe('project setup', () => {
  it('runs tests', () => {
    expect(2 + 2).toBe(4)
  })
})
